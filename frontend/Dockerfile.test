# Build and run the Vitest test suite.
# Context is the frontend/ directory.

FROM node:22-slim AS test
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
CMD ["sh", "-c", "npm test; status=$?; if [ $status -ne 0 ]; then exit $status; fi; sleep 5"]
